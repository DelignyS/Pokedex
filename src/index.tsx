import React from "react";
import ReactDOM from "react-dom";
import App from './App'; 

ReactDOM.render( 
<App />,  //It'll inject the App component into the div with the id of root in the index.html file.
document.getElementById("root")
);